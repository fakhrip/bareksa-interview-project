package routes

import (
	"github.com/uptrace/bunrouter"
)

func ApiRoutes() func(g *bunrouter.Group) {
	return func(group *bunrouter.Group) {
		// group.GET("/:id", handler)
		// group.POST("", handler)
		// group.PUT("/:id", handler)
		// group.DELETE("/:id", handler)
	}
}
